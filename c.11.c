#include<stdio.h>
 int main()
 {
 	int num;
 	printf("enter the num:-");
 	scanf("%d",&num);
 	if(num%2==0)
 	{
 		printf("the entered num is even");
 		
	 }
	 else
	 {
	 	printf("the entered num is odd");
	 }
	 return 0;
 }
